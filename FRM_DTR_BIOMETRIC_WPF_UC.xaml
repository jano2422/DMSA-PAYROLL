<UserControl x:Class="FRM_DTR_BIOMETRIC_WPF_UC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMSA_System"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Upload Button -->
        <StackPanel Orientation="Horizontal" Margin="10">
            <Button x:Name="BtnUploadDTR" Content="Upload DTR PDF" Width="200" Height="40"
                    HorizontalAlignment="Left"
                    Click="BtnUploadDTR_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="DTRDataGrid" Grid.Row="1" AutoGenerateColumns="False"
                  IsReadOnly="True" AlternatingRowBackground="LightGray"
                  Margin="10" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="DateTime" Binding="{Binding DateTime}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
